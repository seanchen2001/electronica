# Copy to .env.local for local `vercel dev`, and set these in Vercel
# (Project → Settings → Environment Variables) for production.
GEMINI_API_KEY=your-google-ai-studio-key
APP_PASSWORD=choose-a-shared-password
